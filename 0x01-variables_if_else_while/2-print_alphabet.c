#include <stdio.h>
/**
 * main - The entry point
 * Return: 0 (Code successful)
 */

int main(void)
{
	char w;

	w = 'a';
	while
		(w <= 'z') {
			putchar(w);
			w++;
		}
	putchar('\n');
	return (0);
}

#include <stdio.h>
/**
 * main - The entry point
 * Return: 0 (code successful)
 */

int main(void)
{
	char v;

	char w;

	v = 'a';
	w = 'A';
	while
		(v <= 'z') {
			putchar(v);
			v++;
		}
	while
		(w <= 'Z') {
			putchar(w);
			w++;
		}
	putchar('\n');
	return (0);
}

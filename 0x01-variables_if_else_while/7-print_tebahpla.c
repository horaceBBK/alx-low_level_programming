#include <stdio.h>
/**
 * main - the programs entry point
 * Return: 0 (code successful)
 */

int main(void)
{
	char m;

	m = 'z';
	while
		(m >= 'a') {
			putchar(m);
			m--;
		}
	putchar('\n');
	return (0);
}

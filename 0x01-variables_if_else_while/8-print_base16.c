#include <stdio.h>
/**
 * main - the  programs entry point
 * Return: 0 (code successful)
 */

int main(void)
{
	char x;

	int y;

	x = 'a';
	y = 0;
	while
		(y < 10) {
			putchar(y + '0');
			y++;
		}
	while
		(x <= 'f') {
			putchar(x);
			x++;
		}
	putchar('\n');
	return (0);
}

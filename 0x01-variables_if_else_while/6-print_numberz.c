#include <stdio.h>
/**
 * main - the programs entry point
 * Return: 0 (code successful)
 */

int main(void)
{
	int g;

	g = 0;
	while
		(g < 10) {
			putchar(g + '0');
			g++;
		}
	putchar('\n');
	return (0);
}

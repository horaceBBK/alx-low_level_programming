#include <stdio.h>
/**
 * main - With this programme, we  will print a line using the printf function
 * return 0 (code succesfully executed)
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
